---
title: 404 - 页面走丢了
aside: false
---

<script setup lang="ts">
import { useRoute, useData } from 'vitepress'
import { computed } from 'vue'

const route = useRoute()
const { site } = useData()

// 把所有分类的 sections 拿过来给 404 推荐（从 localStorage 缓存 / 全站索引）
const allLinks = [
  { text: '🏠 回到首页', link: '/' },
  { text: '🗂 总目录', link: '/readme' },
  { text: '▤ 读书笔记', link: '/reading-notes' },
  { text: '⌘ 全栈学习', link: '/fullstack' },
  { text: '◈ DevOps', link: '/devops' },
  { text: '✦ Agent / AI', link: '/agent' },
  { text: '◉ 个人成长', link: '/career' },
  { text: '⬡ 技术地图', link: '/roadmap' },
]
</script>

<div class="not-found">
  <div class="not-found-decoration">404</div>
  <h1>这里没有内容</h1>
  <p class="lead">
    你访问的路径 <code>{{ route.path }}</code> 不存在，
    或者还没被收录到知识图谱里。
  </p>
  <p class="hint">要不，从下面这些入口开始？</p>
  <ul class="suggestions">
    <li v-for="l in allLinks" :key="l.link">
      <a :href="l.link">{{ l.text }}</a>
    </li>
  </ul>
  <p class="help">
    还找不到？试试顶部 <kbd>⌘ K</kbd> 全文搜索。
  </p>
</div>

<style scoped>
.not-found {
  max-width: 640px;
  margin: 80px auto;
  padding: 0 24px;
  text-align: center;
}

.not-found-decoration {
  font-size: 140px;
  font-weight: 800;
  line-height: 1;
  background: linear-gradient(135deg, #0f7b53, #25b47e 50%, #225e8b);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
  margin-bottom: 20px;
  letter-spacing: -0.04em;
}

.dark .not-found-decoration {
  background: linear-gradient(135deg, #5fddae, #8ee8c5 50%, #5fa8e8);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}

.not-found h1 {
  font-size: 32px;
  margin: 0 0 16px;
  color: var(--vp-c-text-1);
}

.lead {
  font-size: 16px;
  color: var(--vp-c-text-2);
  margin-bottom: 32px;
  line-height: 1.7;
}

.lead code {
  background: var(--vp-c-bg-soft);
  border: 1px solid var(--vp-c-divider);
  padding: 2px 8px;
  border-radius: 5px;
  font-size: 14px;
  color: var(--vp-c-text-1);
}

.hint {
  font-size: 14px;
  color: var(--vp-c-text-3);
  margin-bottom: 20px;
}

.suggestions {
  list-style: none;
  padding: 0;
  margin: 0 0 32px;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 10px;
}

.suggestions li {
  margin: 0;
}

.suggestions a {
  display: block;
  padding: 14px 18px;
  border: 1px solid var(--vp-c-divider);
  border-radius: 10px;
  text-decoration: none;
  color: var(--vp-c-text-1);
  font-size: 14px;
  font-weight: 500;
  transition: border-color 0.2s ease, transform 0.2s ease, box-shadow 0.2s ease, background-color 0.2s ease;
}

.suggestions a:hover {
  border-color: var(--vp-c-brand-1);
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(23, 59, 44, 0.08);
  background-color: var(--vp-c-bg-soft);
}

.help {
  font-size: 14px;
  color: var(--vp-c-text-3);
  margin: 0;
}

.help kbd {
  background: var(--vp-c-bg-soft);
  border: 1px solid var(--vp-c-divider);
  padding: 2px 8px;
  border-radius: 5px;
  font-family: monospace;
  font-size: 13px;
}

@media (max-width: 640px) {
  .suggestions {
    grid-template-columns: 1fr;
  }
  .not-found-decoration {
    font-size: 96px;
  }
}
</style>
# Node / 构建产物
node_modules/
.npm-cache/

# VitePress 缓存与构建
docs/.vitepress/dist/
docs/.vitepress/cache/

# 编辑器
.idea/
.vscode/

# 本地设计与隔离 worktree
.superpowers/
.worktrees/

# 临时导出
docs-八股文旧版备份.tar.gz

# 自动生成的搜索索引
docs/.vitepress/dist/assets/

# GitHub Pages workflow 元数据
!docs/.vitepress/dist/.gitkeep

# sync-toc 自动生成（避免冲突，CI 会重跑）
docs/.vitepress/theme/sections.json

# 内部规范与决策记录（外部用户无需看到）
docs/writing-skills/
docs/conventions/
docs/adr/

# AI onboarding 入口（本地维护，外部用户无需看到）
.ai/

# 内部规范与决策记录（外部用户无需看到）
docs/writing-skills/
docs/conventions/
docs/adr/

# 跨大模型一致性 SOP（本地维护，不公开）
_internal/
